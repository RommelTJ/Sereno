import ViewStub from '../components/ViewStub.tsx'

function Withdrawals() {
  return (
    <ViewStub testId="view-withdrawals">
      The tax-aware sequencing waterfall — ETH inside the 0% long-term-gains
      headroom, then brokerage, then 401(k) — lands here.
    </ViewStub>
  )
}

export default Withdrawals
