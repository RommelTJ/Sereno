import ViewStub from '../components/ViewStub.tsx'

function Ledger() {
  return (
    <ViewStub testId="view-ledger">
      One row per month per account — the monthly balance table and entry form
      land here.
    </ViewStub>
  )
}

export default Ledger
