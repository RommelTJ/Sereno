import ViewStub from '../components/ViewStub.tsx'

function Guardrails() {
  return (
    <ViewStub testId="view-guardrails">
      Guyton-Klinger Cut / Hold / Raise bands around your at-retirement anchor
      rate — the guardrails view lands here.
    </ViewStub>
  )
}

export default Guardrails
